---
applyTo: "**"
---
# Global Instructions
- Prefer C# language, readability, dockerfile, yaml, markdown, and bash scripts when applicable, kubernetes.
- For configuration values, use environment variables and provide clear error messages if they are missing.
- Avoid new dependencies unless requested
- Respect module boundaries
